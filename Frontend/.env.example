# API Configuration
# Base URL for backend API requests
# In development with Vite proxy: /api/v1
# In production: https://api.yourdomain.com/api/v1
VITE_API_URL=/api/v1

# WebSocket Configuration
# Leave empty to connect to same origin (recommended for production behind reverse proxy)
# For development without proxy: http://localhost:8000
VITE_SOCKET_URL=
