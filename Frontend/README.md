# NPIP Frontend (MVP)

Minimal React UI for the Nepal Public Figure Intelligence Platform.

## Setup

1. `npm install`
2. `npm run dev`

Frontend runs on `http://localhost:5173` and proxies `/api` to the backend.
